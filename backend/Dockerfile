# Use the official Node.js 18 Alpine image.
FROM node:18-alpine

# Set the working directory to /app/backend
WORKDIR /app/backend

# Expose the port the app runs on
EXPOSE 3000

# Add your application source code here (optional)
# COPY . .  
# RUN npm install  
# CMD [ "node", "index.js" ]  
